// == igual a, testa somento o valor
// != diferente de, testa somento o valor

var x = 0;

1 == 1; //retorna true

1 == 2; //retorna false

1 != 2; //retorna true

2 != 2; //retorna false

x == 0; //retorna true

"joao" == "joao"; //retorna true pos sao valores iguais

var nome = "joao";

nome == "joao"; //retorna true pos sao valores iguais

var pessoa = "maria";

nome == pessoa; //retorna false pos sao valores diferente

1 == "1"; //retorna true, porque os valores são iguais

1 === "1"; //retorna false, porque os valores são iguais, mas os tipos são  diferentes

1 !== "1"; //retorna true, porque os tipos são  diferentes

// > maior que
// < menor que
// >= maior ou igual
// <= menor ou igual

2 > 1; // retorna true
1 < 2; // retorna true
2 >= 2; //returna true
2 <= 2; //returna true
